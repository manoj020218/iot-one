# Jenix One — Device Provisioning Standard (moved)

This document now lives at the repo root: **`../../PROVISIONING.md`**
(from this file's location, that's `jenix One/PROVISIONING.md`).

This local copy was a fork that drifted from the root version for several
weeks (the root copy still carried a claim later proven false in Section
5, "Correction, 2026-08-27," while this copy had the fix) before being
found and reconciled on 2026-09-11. Every device's relative-path reference
(`../../PROVISIONING.md`) resolves to the root file, so that is the one
that must stay canonical — **do not re-add content here.** Edit the root
file directly, including for QRunlock-specific sections (the QRunlock and
School Bell delta sections both live there now, not split across copies).
