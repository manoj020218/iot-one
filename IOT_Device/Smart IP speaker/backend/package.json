{
  "name": "@jenix/ip-speaker-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jenix IP Speaker backend module for Jenix One. Self-contained package with routers, services, in-memory stores and platform dependency injection.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@jenix/device-schemas": "workspace:*",
    "@jenix/shared": "workspace:*",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.20.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  }
}
