# Build artifacts, not source -- same convention as ../android/.gitignore.
# Regenerate via `./gradlew bundleRelease` (see ../RELEASE_SIGNING.md) rather
# than committing a 15-30MB binary to git history.
*.aab
*.apk
