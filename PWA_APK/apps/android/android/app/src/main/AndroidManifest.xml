<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- @capacitor-community/bluetooth-le's own AAR manifest declares these
         with maxSdkVersion="30" (correct per Android's own BLUETOOTH_SCAN/
         BLUETOOTH_CONNECT migration guidance), but Capacitor's generic
         requestPermissions() checks every alias in the plugin's
         @CapacitorPlugin(permissions=[...]) annotation for bare presence in
         the merged manifest, and BLUETOOTH/BLUETOOTH_ADMIN, which are capped
         at API 30, are correctly absent from PackageManager's
         requestedPermissions on newer devices, so that check always fails
         there and requestPermissions() rejects before any real (Scan/
         Connect) permission dialog appears. Declaring them here without
         the cap is a harmless no-op on API 31+ (BLUETOOTH_SCAN/CONNECT are
         what the OS actually enforces) but satisfies the presence check on
         every API level. -->
    <uses-permission android:name="android.permission.BLUETOOTH" tools:node="replace" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" tools:node="replace" />

    <!-- WifiInfoPlugin: reading the phone's currently-connected SSID (to
         prefill the Wi-Fi provisioning form) requires location permission
         on every Android version. Unlike BLE scanning above, there is no
         neverForLocation-style opt-out for this specific API. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>
